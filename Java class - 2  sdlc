#####JAR FILE##############

jar == java archive
jar file is all about zipping the class files(byte codes) .
jar file is a library itself.cz java is a machine independent language.so if we make a jar app then it will run on any device which supports java.
jar file e akta manifest lagbe karon kon class ta main eta to r java janena..eta manifest er moddhe likhe dile java khuje nibe. name == manifest.mf
command == 


#######################################


        #########C diye java er native code##########3
        
        
        javar sathe C er communication korte hole native methode nite hobe.
        firstly class gula create kore nite hobe(compile kore).
        javah(java header) er maddhome header file(obviously java header file)  create korte hbe and erpor same nam e akta C file create korte hobe.
        secondly shared_object(linux) / dll(windows) e create korte hobe.
        then GNI er folder er moddhe implement korte java file theke shei class k define korlei java te C program ti run korbe.
        Mainly c/c++ code gulai shared_object(SO)/DLL file hoye oikhan theke load hocche..akbar SO/dll file create howar por setai run hobe.
        
        c/c++ gula run hobe na.
        
        
#################################################3
